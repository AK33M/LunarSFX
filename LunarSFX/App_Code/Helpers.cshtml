@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<LunarSFX.Core.Objects.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @LunarSFX.Extensions.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
    }
}